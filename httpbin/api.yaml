type: api
version: v4.2.0
data:
  name: httpbin-test
  context: /httpbin-test
  version: 1.0.0
  provider: admin
  lifeCycleStatus: CREATED
  isRevision: false
  revisionId: 0
  type: HTTP
  transport:
  - http
  - https
  policies:
  - Unlimited
  visibility: PUBLIC
  endpointConfig:
    endpoint_type: http
    production_endpoints:
      url: http://httpbin.org
  endpointImplementationType: ENDPOINT
  websubSubscriptionConfiguration: null
